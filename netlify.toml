# Next.js 16 on Netlify — OpenNext adapter is applied automatically.
# Docs: https://docs.netlify.com/build/frameworks/framework-setup-guides/nextjs/overview/

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Server Actions + dynamic booking pages need the Node serverless runtime (default for App Router).
